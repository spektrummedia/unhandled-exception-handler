image: Visual Studio 2019

branches:
  only:
    - master
    - develop

install:
  - set PATH=C:\Program Files (x86)\MSBuild\14.0\Bin;%PATH%

before_build:
  - cd src
  - nuget restore

build:
  project: src/Spk.UnhandledExceptionHandler.sln
  publish_nuget: true

after_build:
  - nuget pack Spk.UnhandledExceptionHandlerCore/Spk.UnhandledExceptionHandlerCore.csproj

artifacts:
  - path: 'src\Spk.UnhandledExceptionHandlerCore.*.nupkg'

deploy:
  provider: NuGet
  api_key:
    secure: yydaaicCRjoEkJfA82Ajb5rpP2UKdl8rj85V+gof+Ss=
  artifact: /.*\.nupkg/
  on:
    branch: master